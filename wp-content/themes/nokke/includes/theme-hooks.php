<?php
/**
 * Theme hooks.
 * @author  	 DeoThemes
 * @copyright  (c) Copyright by DeoThemes
 * @link       https://deothemes.com
 * @package 	 Nokke
 * @since 		 1.0.0
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit( 'Direct script access denied.' );
}

/**
 * Head top
 */
function nokke_head_top() {
	do_action( 'nokke_head_top' );
}

/**
 * Head bottom
 */
function nokke_head_bottom() {
	do_action( 'nokke_head_bottom' );
}

/**
 * Header before
 */
function nokke_header_before() {
	do_action( 'nokke_header_before' );
}

/**
 * Site Header
 */
function nokke_header() {
	do_action( 'nokke_header' );
}

/**
 * Header after
 */
function nokke_header_after() {
	do_action( 'nokke_header_after' );
}

/**
 * Masthead
 */
function nokke_masthead() {
	do_action( 'nokke_masthead' );
}

/**
 * Menu before
 */
function nokke_menu_before() {
	do_action( 'nokke_menu_before' );
}

/**
 * Menu after
 */
function nokke_menu_after() {
	do_action( 'nokke_menu_after' );
}

/**
 * Last menu item after
 */
function nokke_last_menu_item_after() {
	do_action( 'nokke_last_menu_item_after' );
}

/**
 * Body top
 */
function nokke_body_top() {
	do_action( 'nokke_body_top' );
}

/**
 * Body bottom
 */
function nokke_body_bottom() {
	do_action( 'nokke_body_bottom' );
}

/**
 * Primary content top
 */
function nokke_primary_content_top() {
	do_action( 'nokke_primary_content_top' );
}

/**
 * Primary content before
 */
function nokke_primary_content_before() {
	do_action( 'nokke_primary_content_before' );
}

/**
 * Primary content query
 */
function nokke_primary_content_query() {
	do_action( 'nokke_primary_content_query' );
}

/**
 * Primary content after
 */
function nokke_primary_content_after() {
	do_action( 'nokke_primary_content_after' );
}

/**
 * Primary content bottom
 */
function nokke_primary_content_bottom() {
	do_action( 'nokke_primary_content_bottom' );
}

/**
 * Comments before
 */
function nokke_comments_before() {
	do_action( 'nokke_comments_before' );
}

/**
 * Comments
 */
function nokke_comments() {
	do_action( 'nokke_comments' );
}

/**
 * Comments after
 */
function nokke_comments_after() {
	do_action( 'nokke_comments_after' );
}

/**
 * Entry section before
 */
function nokke_entry_section_before() {
	do_action( 'nokke_entry_section_before' );
}


/**
 * Content before
 */
function nokke_content_before() {
	do_action( 'nokke_content_before' );
}

/**
 * Content top
 */
function nokke_content_top() {
	do_action( 'nokke_content_top' );
}

/**
 * Content bottom
 */
function nokke_content_bottom() {
	do_action( 'nokke_content_bottom' );
}

/**
 * Content after
 */
function nokke_content_after() {
	do_action( 'nokke_content_after' );
}


/**
 * Entry featured image before
 */
function nokke_entry_featured_image_before() {
	do_action( 'nokke_entry_featured_image_before' );
}

/**
 * Entry featured image
 */
function nokke_entry_featured_image() {
	do_action( 'nokke_entry_featured_image' );
}

/**
 * Entry featured image after
 */
function nokke_entry_featured_image_after() {
	do_action( 'nokke_entry_featured_image_after' );
}

/**
 * Entry content top
 */
function nokke_entry_content_top() {
	do_action( 'nokke_entry_content_top' );
}

/**
 * Entry content bottom
 */
function nokke_entry_content_bottom() {
	do_action( 'nokke_entry_content_bottom' );
}

/**
 * Page title before
 */
function nokke_page_title_before() {
	do_action( 'nokke_page_title_before' );
}

/**
 * Page title after
 */
function nokke_page_title_after() {
	do_action( 'nokke_page_title_after' );
}

/**
 * Sidebar before
 */
function nokke_sidebar_before() {
	do_action( 'nokke_sidebar_before' );
}

/**
 * Sidebar after
 */
function nokke_sidebar_after() {
	do_action( 'nokke_sidebar_after' );
}

/**
 * Footer before
 */
function nokke_footer_before() {
	do_action( 'nokke_footer_before' );
}

/**
 * Footer
 */
function nokke_footer() {
	do_action( 'nokke_footer' );
}

/**
 * Footer after
 */
function nokke_footer_after() {
	do_action( 'nokke_footer_after' );
}